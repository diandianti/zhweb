典语言 cat 示例 —— 回显输入
读取字符并原样输出，直到 EOF（输入结束）

原理：读入字符 → 若非零则输出再继续读入 → 零（EOF）则停止
《》   读入第一个字符
急     循环：当前单元非零时继续
绷     输出当前字符
《》   读入下一个字符
乐     循环结束

支持中文及任意 Unicode 字符
运行：echo "你好世界" | python dian_lang.py examples/cat.dian
